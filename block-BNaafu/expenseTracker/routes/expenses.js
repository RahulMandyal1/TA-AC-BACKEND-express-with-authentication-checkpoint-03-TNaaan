const express = require("express");
const Expense = require("../models/expenses");
const router = express.Router();
const Income = require("../models/income");
// home page of all the Expenses
module.exports = router;
